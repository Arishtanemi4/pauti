# Editors / tooling
.vscode/
.dbeaver/
.serena/
*GEMINI.md*
.DS_Store

# headroom MCP runtime state — local PID/port bookkeeping, not project state
.claude/.headroom_wrap_marker.json
.claude/.headroom_wrap_owners.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Node / Expo
node_modules/
.expo/
dist/
build/
web-build/
expo-env.d.ts
.metro-health-check*
*.tsbuildinfo
/android
/ios

# Native signing
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.pem
npm-debug.*
yarn-debug.*
yarn-error.*

# Local databases — one per device, never committed
*.db
*.db-wal
*.db-shm

# Secrets
.env
.env.*
!.env.example

# Personal financial data. Real receipts and bank statements stay on device;
# this repository is intended to be public. See docs/architecture.md ADR-010.
ml/tests/fixtures/receipts/
ml/tests/fixtures/bank-statements/
ml/tests/fixtures/extracts/

# Bug artefact: directory created from an unsubstituted template string
ml/{DB_PATH}/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
