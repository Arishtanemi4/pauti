# Editors / tooling
.vscode/
.dbeaver/
.serena/
*GEMINI.md*

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Node / Expo
node_modules/
.expo/
dist/
build/
web-build/

# Local databases — one per device, never committed
*.db
*.db-wal
*.db-shm

# Secrets
.env
.env.*
!.env.example

# Personal financial data. Real receipts and bank statements stay on device;
# this repository is intended to be public. See docs/architecture.md ADR-010.
backend/docs/data/
ml/tests/fixtures/receipts/
ml/tests/fixtures/bank-statements/
ml/tests/fixtures/extracts/

# Bug artefact: directory created from an unsubstituted template string
backend/{DB_PATH}/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
